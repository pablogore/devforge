// Package tools provides detection and automatic installation of required analysis tools
// (golangci-lint, govulncheck) so that CI and local runs do not require manual tool setup.
// Used by the CLI before running pipeline commands.
package tools
