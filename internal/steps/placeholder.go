// Package steps contains reusable CI/CD step implementations.
//
// Future modules:
//   - lint: linting steps (golangci-lint, etc.)
//   - test: testing steps (go test, coverage)
//   - build: build steps (go build, docker build)
//   - publish: publishing steps (docker push, git tag)
package steps
